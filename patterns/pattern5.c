#include<stdio.h>
int main()
{
	int row;
	int column;
	int key;
	for(row = 5; row >= 1; row--) {
		for(column = 5; column >= 1; column--) {
//		key = row + column -1;
//		if(key > 5)
//		key = key - 5;
		printf("%d%d", row , column);
//		}
		}
	printf(" \n ");
	}
return 0;
}
